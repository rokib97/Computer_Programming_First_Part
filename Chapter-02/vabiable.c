#include<stdio.h>

int main(){

    // int a, b; //declare variable
    // a = 50;
    // b = 60;
    int a = (int)20.50, b = 40, sum ; 
    sum = a + b;
    // printf("%d",sum);
    // printf("Sum is %d", sum);
    printf("%d + %d  = %d", a, b, sum);
    return 0;
}